# 탐님께 — 작업 공유 (2026-09-14)

> 지난 피드백 주신 대로, 앞으로는 **작업 시작 전 계획**과 **작업 후 결과·이슈**를
> 이 문서로 먼저 공유하겠습니다. 지금까지는 이 공유가 없어서 탐님이 무엇이
> 끝났고 무엇을 테스트해야 하는지 알 수 없으셨습니다. 제 잘못입니다.

---

## 1. 이번에 고친 것 — 지금 바로 테스트해 주세요

### ① 이음이가 혼자 계속 말하던 문제 ✅ 고쳤습니다

**원인은 탐님 쪽 분석이 정확했습니다.** 답변이 끝나면 마이크가 자동으로 다시
켜졌고, 그 마이크가 주변 소음·정적을 말로 알아들어 → 다시 답변 → 또 마이크 →
무한 반복이었습니다. 타이핑으로 시작해도 답변 뒤에 마이크가 켜져 똑같았습니다.
(이음이 답변 자체는 한 번만 나왔고, 반복은 전부 화면 쪽 문제였습니다.)

**바꾼 방식: 눌러서 말하기 (push-to-talk).**
이제 **🎤 말씀하기** 를 누르는 동안에만 듣습니다. 자동으로 켜지지 않으니 반복
고리가 생길 수 없습니다. 듣는 중에 다시 누르면 멈춥니다.

소음 거르기도 넣었습니다. 다만 **글자 수로 자르지는 않았습니다** — 한국어에서
"네", "예"는 한 글자짜리 정상 대답이라, 짧다고 버리면 진짜 대답을 버리게
됩니다. 대신 빈 결과와, 브라우저가 스스로 자신 없다고 표시한 결과만 무시합니다.

**테스트 방법**
1. 통화 시작 → 이음이 인사가 끝나면 아래에 **🎤 말씀하기** 버튼이 뜹니다.
2. 질문하고 답을 들은 뒤 **아무것도 하지 말고 30초 기다려 보세요.**
   → 이음이가 더 말하지 않으면 정상입니다.
3. 시끄러운 곳에서도 해 보세요. 예전에는 이때 반복이 시작됐습니다.

---

### ② 문자 번호 입력창이 안 뜨던 문제 ✅ 고쳤습니다

**원인도 정확하셨습니다.** `문자 받기` 버튼이 화면에 있긴 했지만 보이게 만드는
코드가 한 줄도 없었습니다. 게다가 안내 문구와 **같은 자리**에 겹쳐 있었습니다.
즉 이음이는 "문자 받기를 누르세요"라고 말하는데, 누를 버튼이 화면에 없었습니다.

이제 **📩 문자 받기** 버튼이 통화 내내 보입니다. 누르면 번호 키패드가 뜹니다.

**덤으로 하나 더 고쳤습니다.** 어르신이 "이걸로 할게요"라고 고르기 전에 문자를
요청하면, 예전에는 요약만 가고 **정작 필요한 문의 전화번호가 빠졌습니다.**
안내한 자리가 하나뿐이면 그 자리의 전화번호까지 넣어 보내도록 했습니다.
(두 개 이상일 때는 임의로 고르지 않습니다.)

**테스트 방법**
1. 일자리를 물어보고 답을 들으세요.
2. **📩 문자 받기** → 번호 입력 (예: `010-1234-3456`) → 예, 보내주세요.
3. 화면에 뜨는 카드 내용에 **▸ 문의** 전화번호가 있는지 봐 주세요.
   ※ 실제 문자는 나가지 않습니다 (문자 키를 일부러 비워 뒀습니다).

---

### ③ 음성인식이 잘 안 되는 문제 ⚠️ 아직 못 고쳤습니다 — 판단이 필요합니다

이건 버그가 아니라 **선택의 문제**라 제가 혼자 결정하지 않았습니다.

지금은 **브라우저 기본 음성인식**을 씁니다. 무료이고 빠르지만 정확도가 낮고,
특히 어르신 말투·사투리·소음에 약합니다. 탐님이 조용한 책상에서 이미 포기하고
타이핑하셨다면, 실제 경로당에서는 더 나쁠 것이 거의 확실합니다.

**대안은 CLOVA 음성인식입니다.** 서버에 연결은 이미 해 뒀습니다(`/stt`).
정확도는 훨씬 좋지만 ① 비용이 듭니다 ② 응답이 0.5~1초쯤 느려집니다.

**여쭙고 싶은 것:** 정확도를 위해 속도와 비용을 조금 포기해도 될까요?
"된다"고 하시면 바꿔서 비교 결과를 보내드리겠습니다.

> 참고로 키오스크 사양서에 **마이크가 안 보입니다.** 오디오는 "1W 스피커"만
> 적혀 있고 입력 장치가 없습니다. 음성 키오스크라 이건 먼저 확인이 필요합니다.
> (3.5mm 단자가 마이크 겸용인지, USB 마이크를 달 수 있는지)
> 그리고 키오스크가 **크롬**으로 도는지 **웹뷰**로 도는지도 알려주세요 —
> 웹뷰라면 브라우저 음성인식이 아예 동작하지 않습니다.

---

## 2. 지난번에 끝냈는데 공유가 부족했던 것

탐님이 "무엇이 끝났는지 모르겠다"고 하신 게 맞습니다. 아래는 이미 들어가
있지만 제대로 안내드리지 못한 것들입니다.

| 무엇 | 어떻게 확인하나 |
|---|---|
| 보내주신 서비스 목록(공통 11 / 서초전용 39) 반영 | 관리자 → ⭐ 서비스 우선순위. 각 항목에 담당기관·링크가 보입니다 |
| 목록 업데이트 방법 | 같은 화면 → 📥 서비스 목록 가져오기. JSON 붙여넣고 **미리보기** → **반영하기**. 배포 필요 없음 |
| 강서가 서초 기관을 물려받던 문제 | master로 로그인 → 상단에서 **강서** 선택 → 서비스가 **15개**만 보입니다 (전에는 52개) |
| 어르신이 말한 지역으로 답하기 | 키오스크에서 "강남구에 일자리 있어요?" → 실제 강남구 공고가 나옵니다 |
| 목록에 없으면 일반 상식으로 답하기 | "감기 걸렸을 때 뭘 먹어요?" → 답합니다. "동사무소 전화번호?" → 답하지 않고 담당자 연결 |

**일반 상식 답변의 경계를 하나 말씀드립니다.** 전화번호·주소·금액·날짜·신청
자격은 일반 상식으로도 **답하지 않게** 해 두었습니다. 이건 "지금 이 동네의
구체적인 사실"이라 지어낼 수 없기 때문입니다. 어르신이 그 번호로 실제로
전화를 거시기 때문에, 틀린 번호는 없는 것보다 나쁩니다.
(건강·의료는 법률 검토 중이라고 하셔서, 복지관별로 **끄는 설정**도 넣었습니다.)

---

## 3. 다음 작업 계획 — 이대로 진행할까요?

주신 실시간 정보 연동(날씨·버스·병원)에 대한 제 계획입니다.
**시작 전에 확인받고 싶습니다.**

**① 먼저 여쭐 것 — 서비스별 API 매핑**
"어떤 서비스를 어느 API로 연동할지 목록을 곧 전달하겠다"고 하셨습니다.
그 목록을 받고 시작하겠습니다. 59개 서비스 중 어느 것이 어느 API에 붙는지
제가 추측하면 틀릴 가능성이 큽니다.

**② 구조 — 실시간 호출이 아니라 미리 받아두기**
일자리(data.go.kr)를 할 때 확인한 것인데, 이 API들은 **응답이 9~30초** 걸렸습니다.
어르신이 기다리는 중에 호출하면 대화가 멈춥니다. 그래서 일자리는 밤에 미리
받아 두고 저장해서 씁니다. 날씨·버스도 같은 방식이 필요할 것 같습니다.
**다만 날씨·버스는 "지금" 값이 중요하므로** 짧은 주기(예: 5~10분) 캐시로
하려 합니다. 이 방향이 맞는지 봐 주세요.

**③ 확인이 필요한 위험**
- 제 쪽 네트워크에서 이 API들이 **실제로 닿는지** 먼저 확인하겠습니다.
  data.go.kr은 느리긴 해도 됐습니다만, 나머지는 아직 모릅니다. 막히면
  바로 말씀드리겠습니다 — 그 부분은 한국 쪽에서 맡아주신다고 하셨습니다.
- **약국 데이터는 API가 아니라 ZIP 파일**입니다 (분기별). 이건 자동 연동이
  아니라 주기적으로 받아서 넣는 방식이 됩니다. 파일 보내주시면 넣겠습니다.

**④ 순서 제안**
주신 "지역 우선 → 일반 답변 후순위" 규칙은 지금 일자리에만 적용돼 있습니다.
실시간 정보에도 같은 규칙을 적용하려면, 서비스 하나씩 붙이는 게 안전합니다.
**날씨 → 병원 → 버스** 순서를 제안드립니다. 날씨가 가장 단순하고, 버스는
정류장을 어떻게 알아낼지(어르신이 정류장 이름을 말해야 하는지)가 관건이라
가장 복잡합니다.

---

## 4. 지금 가장 큰 걸림돌 — 배포

태리 PD님이 두 번 말씀하셨듯, **지금은 제 컴퓨터에서만 돌아갑니다.**
그래서 여러분이 직접 확인할 방법이 없고, 그게 이번 피드백의 근본 원인이라고
생각합니다.

**필요한 것:** Render 계정 + 월 $7. 설정 파일(`render.yaml`)과 순서
(`DEPLOY.md`)는 이미 다 준비돼 있습니다. 계정만 만들어 주시면 30분 안에
링크를 드릴 수 있습니다.

그 전에 제가 해야 할 것: 데이터베이스 비밀번호와 테스트 계정 비밀번호 교체
(지금 것들이 대화창에 노출돼 있어서 공개 주소에 올리면 안 됩니다).

우선은 **.bat 파일**을 만들어서, 더블클릭만 하면 각자 컴퓨터에서 실행되도록
해 보겠습니다. 다만 이건 임시방편이고, 제대로 된 답은 배포입니다.

---

## 5. 프로젝트 이해 — 여쭤보신 것에 대한 답

제가 이해하는 이음이는 이렇습니다.

**한 복지관용 도구가 아니라, 전국 복지관에 공급하는 SaaS입니다.** 그래서
데이터가 복지관별로 분리돼 있어야 하고, 전국 공통 내용은 Play4가 관리하고
복지관은 자기 지역 것만 얹는 구조여야 합니다. 이번 scope(공통/서초전용) 작업이
바로 그 구조를 실제로 맞춘 것입니다. 강서가 방배노인종합복지관을 물려받으면
안 되는 이유가 여기 있습니다.

**돈이 되는 지점은 "복잡한 관리는 Play4가, 쉬운 일만 복지관이" 입니다.** 그래서
담당자 화면은 명단 입력만 남기고, 관리자 화면에 설정을 모았습니다.

**어르신 쪽에서 가장 중요한 것은 속도와 정확성입니다.** 말하고 2~3초 안에
소리가 나와야 대화가 됩니다. 그래서 답변을 스트리밍하고, 문장 단위로 음성을
만들고, 생각(thinking)을 껐습니다. 정확성은 "모르면 모른다고 말하기"입니다 —
급여 정보가 없는데 지어내면, 어르신이 헛걸음하십니다.

**개발 단계**: 기반(P0·P1)은 끝났습니다. 지금은 내용(P2)을 채우는 단계이고,
실시간 정보 연동이 그 다음 큰 덩어리입니다. 남은 큰 미지수는 두 가지 —
**실제 어르신 음성인식**과 **키오스크 하드웨어(마이크)** 입니다. 둘 다 책상에서는
판단할 수 없고, 현장에서 한 번 해 봐야 압니다.

---

## 6. 어려운 점 — 도와주신다고 하셔서 솔직히 말씀드립니다

1. **배포가 막혀 있는 게 제일 큽니다.** 위 4번입니다.
2. **한국 공공 API 접근.** 제 네트워크에서 느리거나 막힐 수 있습니다.
   확인해 보고 막히면 바로 말씀드리겠습니다.
3. **현장 정보가 없습니다.** 실제 경로당 소음이 어느 정도인지, 어르신이
   화면을 어느 거리에서 보시는지, 키오스크에 마이크가 있는지 — 이건 제가
   알 수 없습니다. 한 번이라도 현장 영상이나 사진을 주시면 크게 도움이 됩니다.
4. **공유 방식은 제 잘못이었습니다.** 앞으로 이 문서로 먼저 드리겠습니다.
