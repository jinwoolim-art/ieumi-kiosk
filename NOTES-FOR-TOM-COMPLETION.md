# 탐님께 — 1차 작업 완료 보고 (2026-09-15)

> 먼저 드린 계획서(`NOTES-FOR-TOM.md`)의 **3단계 "끝난 뒤 보고"**입니다.
> 계획서의 1·2·3번을 끝냈습니다. 4번(실시간 연동)은 아직 시작하지 않았고,
> 그 이유도 아래에 적었습니다.

---

## 0. 먼저 — 지난 문서에서 제가 틀리게 드린 숫자 두 가지

만들기 전에 추정치로 말씀드렸던 것을 실제로 재어 보니 달랐습니다.
탐님이 이 숫자로 판단하실 수 있으니 먼저 바로잡습니다.

**① 지시문 크기 — 약 3,140 토큰이라고 말씀드렸는데, 실제로는 7,097 토큰입니다.**
제가 글자 수로 어림잡았던 것이 문제였습니다. 한국어는 어림짐작보다 토큰이
훨씬 많이 듭니다. 이번에는 API 가 직접 세어 준 값입니다.

**② "캐싱하면 첫 소리까지의 시간도 줄어듭니다"라고 말씀드렸는데, 줄지 않습니다.**
16회씩 번갈아 재어 본 결과입니다.

| | 첫 소리까지 (중앙값) | 평균 |
|---|---|---|
| 캐싱 없이 | 1,606ms | 1,690ms |
| 캐싱 켜고 | 1,517ms | 1,668ms |

차이가 거의 없고 분포가 겹칩니다. **캐싱은 비용을 줄이는 수단이지 속도를 줄이는
수단이 아닙니다.**

여기서 중요한 사실이 하나 따라 나옵니다. 지시문을 7,000 토큰이나 덜어냈는데도
속도가 그대로라는 것은, **이음이가 느린 원인이 지시문 크기가 아니라는 뜻**입니다.
그러니 "서비스 목록을 줄이면 빨라지지 않을까"도 답이 아닙니다. 속도 문제는
따로 봐야 하고, 제 생각엔 서버 위치(아래 4번)가 더 큰 몫입니다.

---

## 1. 서비스 기본 켜짐 — 완료 (탐님 최우선 항목)

**가져오기(import)한 서비스는 이제 기본으로 켜집니다.**

### 무엇이 달라졌나
- 새로 추가되는 서비스 → 켜진 상태로 들어옵니다.
- **이미 들어와 있지만 꺼져 있는 서비스** → 같은 파일을 다시 가져오기 하면 켜집니다.
- 처음 설치(seed)할 때도 켜진 상태로 깔립니다.

두 번째 항목이 중요합니다. "새로 추가되는 것만 켜기"로 만들었다면, 탐님처럼
**이미 60개를 넣어 두고 전부 꺼져 있는 상태**에서는 빠져나올 방법이 없습니다.
파일을 다시 넣어도 "변경 없음 60건"만 나오고 아무것도 안 켜졌을 겁니다.

### 그 대신 생기는 위험과, 그에 대한 대비
이 방식은 **복지관이 일부러 꺼 둔 서비스도 다시 켭니다.** 그래서 조용히
켜지지 않도록 세 가지를 같이 넣었습니다.

1. **미리보기에 "켜지는 서비스 N건"이 먼저 나옵니다** — 어떤 코드인지까지.
2. **체크박스 "가져온 서비스를 바로 켜기"** — 체크를 풀면 목록만 들어오고
   어떤 스위치도 건드리지 않습니다.
3. 변경 사항이 없고 **켜지기만 하는 경우에도 [반영하기] 버튼이 활성화**됩니다.
   (예전에는 "변경 없음"이면 버튼이 꺼져 있어서 켤 방법이 없었습니다.)

### 제가 판단해서 진행한 것 — ⓐ
계획서에서 여쭤본 **ⓐ(전국 공통 서비스도 켤 것인가)** 는 답을 기다리지 않고
**제가 권해드린 대로 "켜는 쪽"으로 먼저 만들었습니다.** 전국 공통 서비스는
그것을 물려받은 모든 복지관에서 켜집니다. 강서 복지관의 15개도 대상입니다.

되돌리기는 쉽습니다. 다르게 생각하시면 말씀만 주세요.

### 만들다가 발견해서 같이 고친 것
처음 만든 방식에 **복지관 경계를 넘는 버그**가 있었습니다. 서비스 코드(s19 같은)는
복지관마다 따로 존재할 수 있는데, 코드만 보고 켜도록 짜는 바람에 **강서에서
가져오기를 하면 서초의 스위치까지 켜지는** 상태였습니다. 실제 사고가 나기 전에
잡았고, 이 경우만 잡아내는 시험을 따로 넣어 두었습니다.

---

## 2. 프롬프트 캐싱 — 완료

실제 측정값입니다. 탐님 서비스 60개를 모두 켠 상태입니다.

| | 새로 보낸 양 | 캐시에 기록 | 캐시에서 읽음 |
|---|---|---|---|
| 캐싱 없이 (매 턴) | 7,097 | 0 | 0 |
| 캐싱 켜고 — 첫 턴 | 180 | 6,917 | 0 |
| 캐싱 켜고 — 이후 모든 턴 | 약 185 | 0 | 6,917 |

캐시에 기록할 때는 보통 입력의 1.25배, 읽을 때는 **0.1배**가 듭니다. 그래서
**대화 한 번만으로도 이득입니다.** 인사 + 질문 4개짜리 대화 하나를 기준으로
지시문 비용이 **5.0배 → 1.65배**로 줄고, 캐시가 살아 있는 동안은 턴당 약 **8배**
저렴합니다. 캐시는 5분간 유지되므로, 손님이 뜸한 시간대에는 가끔 다시 기록하게
됩니다. 그래도 캐싱을 안 하는 것보다 쌉니다.

계획서에서 "복지관 이름·말투를 뒤로 옮겨야 한다"고 말씀드렸는데,
**그건 하지 않았습니다.** 복지관별 부분까지 통째로 캐시에 넣는 쪽이
훨씬 이득이기 때문입니다 — 규칙만 복지관끼리 공유하면 약 1,000 토큰을 아끼지만,
복지관별 부분까지 캐시하면 6,917 토큰을 아낍니다. 복지관 수가 아주 많아지고
각 복지관 이용이 아주 뜸해지면 그때 다시 보겠습니다.

---

## 3. 공공 API 접속 시험 — 완료. **닿습니다.**

탐님이 "이란에서 접속이 되는지"를 물으셨습니다. **전부 닿습니다.**

| 대상 | 결과 | 응답 시간 |
|---|---|---|
| 일자리 (지금 쓰는 키, 대조군) | **정상 200** | 8,794ms |
| 기상청 단기예보 | 403 — 등록되지 않은 서비스키 | 370ms |
| 국토부 TAGO 버스도착 | 403 — 등록되지 않은 서비스키 | 355ms |
| 심평원 병원정보 | 403 — 등록되지 않은 서비스키 | 364ms |
| 국립중앙의료원 응급의료 | 403 — 등록되지 않은 서비스키 | 349ms |
| 서울 열린데이터 버스 | 401 — 유효하지 않은 서비스키 | 1,421ms |

주소 확인(DNS)·접속·응답 모두 정상입니다. **차단이나 시간 초과는 하나도
없었습니다.** 403/401 은 네트워크 문제가 아니라 **키 문제**입니다. 제 쪽에는
지금 일자리용 키 하나만 들어가 있고, 그 키는 일자리 서비스에만 등록되어 있어서
나머지 4개에서는 거부됩니다.

**여기서 두 가지를 알 수 있습니다.**

1. **한국까지 왕복이 약 350ms 입니다.** 거리 자체는 문제가 아닙니다.
2. **일자리 API 의 8.8초는 거리 때문이 아니라 data.go.kr 쪽 처리 시간입니다.**
   거부된 요청은 350ms 에 돌아왔으니까요.

이 구분이 설계를 가릅니다. **날씨처럼 빠른 것은 대화 중에 바로 불러도 되지만,
일자리처럼 느린 것은 미리 받아 두어야 합니다.** 나머지 4개가 어느 쪽인지는
키가 들어와야 잴 수 있습니다.

### 그래서 지금 막혀 있는 것 — 키
발급해 주신 키가 아직 제 쪽 설정 파일에 들어 있지 않습니다.
**단, 채팅으로 다시 보내지는 말아 주세요.** 이미 한 번 대화창에 올라온 키는
노출된 것으로 보아야 하고, 그건 Neon DB 비밀번호도 마찬가지입니다
(배포 전에 둘 다 교체해야 합니다). 전달 방법만 정해 주시면 맞추겠습니다.

**실시간 연동(계획서 4번)을 시작하지 않은 이유가 이것입니다.** 닿는지부터
확인하고 보고드리겠다고 말씀드렸고, 그 결과가 위 표입니다.

---

## 4. 테스트 방법 — 무엇을 어떻게 확인하시면 되는지

탐님이 "무엇이 끝났고 무엇을 시험해야 할지 알 수 없다"고 하신 점, 이번엔
구체적으로 적습니다.

**⚠ 먼저 알아 두실 것: 서초 복지관에서는 달라진 게 안 보입니다.**
이미 60개를 손으로 다 켜 두셨기 때문입니다. 확인하시려면 둘 중 하나로:

**A. 강서 복지관으로 확인 (가장 확실)**
1. 마스터로 로그인 → 복지관을 **강서**로 선택
2. 지금은 물려받은 15개가 **전부 꺼짐**입니다
3. 서비스 목록 파일을 **미리보기** → "**켜짐 15건**"이 표시되는지 보세요
4. **반영하기** → 15개가 켜집니다
5. 강서 키오스크에서 그 서비스를 물어보면 이제 안내합니다

**B. 서초에서 체크박스만 확인**
1. 가져오기 화면에 **"가져온 서비스를 바로 켜기"** 체크박스가 새로 있습니다
2. 체크를 **푼 채로** 미리보기 → "켜짐" 건수가 안 나옵니다
3. 체크를 **한 채로** 미리보기 → 켜질 건수와 코드가 나옵니다

**캐싱은 눈으로 보이지 않습니다.** 대화가 예전과 똑같이 동작하면 정상입니다.
효과는 비용 청구서에 나타납니다. (속도는 위 0-② 대로 달라지지 않습니다.)

시험은 자동 검사 **98개 전부 통과** 상태입니다. 이번에 6개를 새로 넣었고,
그중 하나는 위에서 말씀드린 복지관 경계 버그를 잡아내는 것입니다.

---

## 5. 남은 이슈와, 탐님께 부탁드릴 것

### 제가 기다리고 있는 것
1. **API 키 전달** — 이게 실시간 연동의 시작점입니다. (채팅 말고 다른 방법으로)
2. **ⓑ 판단** — 버스는 "어느 정류장인지"를 알아야 합니다.
   어르신께 정류장 이름을 여쭐까요, 아니면 **복지관 앞 정류장으로 고정**할까요?
   저는 고정을 권합니다. 어르신이 정류장 이름을 정확히 말씀하시기는 어렵습니다.
3. **ⓐ 재확인** — 위 1번대로 "공통 서비스도 켜짐"으로 만들었습니다. 괜찮으신지만.

### 자료 쪽에서 부탁드릴 것 (개발 없이 바로 반영됩니다)
1. **s2 설명·검색어에서 "우울증" 삭제** — 지금은 s2 와 s35 가 둘 다 우울증을
   가지고 있어서 어느 쪽이 나와도 자료대로입니다.
2. **이름이 겹치는 23개에 지역명 추가** — 그중 9개가 전부 "노인복지서비스"라
   똑같은 이름입니다. 어르신이 물으시면 이음이가 구별할 방법이 없습니다.
   예: `노인복지서비스(방배)`

### 그대로 남아 있는 것
- **배포** — 아직 아무 곳에도 올라가 있지 않습니다. 태리PD님이 시험하시려면
  제 컴퓨터가 켜져 있어야 합니다. 한국/일본 리전에 올리면 링크 하나로
  해결되고, 공공 API 호출도 한국에서 나갑니다. Render 계정과 월 $7 입니다.
- **비밀번호 교체** — DB 비밀번호와 로그인 비밀번호 3개. 공개 주소가 생기기 전에.
- **키오스크 가로 화면(21.5")** — 아직입니다.
- **마이크** — 안드로이드 기기가 크롬인지 웹뷰인지에 따라 음성인식 가능 여부가
  갈립니다. 하드웨어 담당자께 확인 부탁드립니다.

---

## 6. 다음 순서 제안

키가 들어오는 대로 **날씨부터** 하겠습니다. 가장 빠르고, 가장 자주 묻는 질문이고,
탐님이 말씀하신 "GPT에게 오늘 날씨 묻는 수준"에 가장 가깝습니다.
그다음 병원/응급, 마지막이 버스입니다. (버스가 가장 까다롭습니다 — 정류장 문제.)

다만 **배포를 먼저 하는 편이 낫다**는 생각은 그대로입니다. 실시간 연동은
"한국에서 API를 부르는 것"이 전제인데, 배포하면 그게 자동으로 해결됩니다.

중간에 판단이 필요한 갈림길이 나오면 그때그때 여쭙겠습니다.
