# 변경 사항 목록 — 2026-09-15

> 상세 설명과 측정값은 `NOTES-FOR-TOM-COMPLETION.md` 에 있습니다.
> 이 문서는 **바뀐 것만** 추린 목록입니다.

---

## 1. 서비스 기본 켜짐 (탐님 최우선 항목)

- **가져오기 한 서비스가 기본으로 켜집니다.** 예전에는 꺼진 채로 들어와서
  안내 자체가 막혔습니다.
- **이미 들어와 있지만 꺼져 있는 서비스도**, 같은 파일을 다시 가져오기 하면
  켜집니다. (지금 서초 60개가 꺼져 있었다면 이 방법으로 한 번에 켜집니다.)
- **전국 공통 서비스는 그것을 물려받은 모든 복지관에서 켜집니다.**
  → 강서 복지관의 15개도 대상입니다. (계획서 ⓐ 를 제가 판단해 진행한 부분)
- **처음 설치할 때도** 서비스가 켜진 상태로 깔립니다.
- **가져오기 화면에 체크박스가 생겼습니다** — "가져온 서비스를 바로 켜기".
  체크를 풀면 목록만 들어오고 어떤 스위치도 건드리지 않습니다.
- **미리보기에 "켜짐 N건"이 표시됩니다** — 어떤 코드가 켜지는지까지 미리 보여줍니다.
- **변경 사항이 없고 켜지기만 하는 경우에도 [반영하기] 버튼이 눌립니다.**
  (예전에는 "변경 없음"이면 버튼이 꺼져서 켤 방법이 없었습니다.)
- 새 서비스 안내 문구에서 "추가된 뒤에는 꺼진 상태입니다"를 삭제했습니다.

## 2. 버그 수정

- **복지관 경계를 넘는 버그를 발견해 고쳤습니다.** 서비스 코드(s19 등)는
  복지관마다 따로 존재할 수 있는데, 코드만 보고 켜도록 되어 있어서
  **강서에서 가져오기를 하면 서초의 스위치까지 켜지는** 상태였습니다.
  실제 사고 전에 잡았고, 이 경우만 잡아내는 시험을 따로 넣었습니다.

## 3. 프롬프트 캐싱 (비용 절감)

- **캐싱을 켰습니다.** 매 대화마다 다시 보내던 7,097 토큰 중 6,917 토큰을
  서버가 기억하고 재사용합니다.
- 캐시가 살아 있는 동안 **턴당 입력 비용 약 1/8**. 대화 한 번만으로도 이득입니다.
- **지시문을 두 덩어리로 나눴습니다** — 복지관마다 고정인 부분(규칙·서비스 목록)과
  질문마다 바뀌는 부분(일자리 목록). 앞쪽만 캐시합니다.
- 캐싱이 거부되는 상황에서도 **대화는 그대로 됩니다** — 캐싱만 빼고 다시 보냅니다.
  프롬프트 내용은 똑같으므로 답변이 달라지지 않습니다.
- 계획서에서 "복지관 이름·말투를 뒤로 옮기겠다"고 했던 것은 **하지 않았습니다.**
  복지관별 부분까지 통째로 캐시하는 쪽이 6배 이상 이득이라서입니다.

## 4. 확인·측정한 것 (코드 변경 없음)

- **공공 API 6곳 전부 접속됩니다.** 차단·시간 초과 없음. 403/401 은 네트워크가
  아니라 **키 등록 문제**입니다.
- **한국까지 왕복 약 350ms.** 일자리 API 의 8.8초는 거리가 아니라
  data.go.kr 쪽 처리 시간입니다.
- **지시문 크기 7,097 토큰** (이전에 약 3,140 이라고 드린 것은 제 추정 오류).
- **캐싱은 속도를 줄이지 않습니다** — 1,606ms → 1,517ms, 차이 거의 없음.
  16회씩 측정. 이음이가 느린 원인은 지시문 크기가 아닙니다.

## 5. 시험

- 자동 검사 **98개 전부 통과**.
- 이번에 **6개를 새로 추가**했습니다:
  - 가져온 서비스가 모든 복지관에서 켜진 상태로 들어오는지
  - 같은 파일을 다시 가져오면 꺼져 있던 것이 켜지는지
  - 미리보기가 켜질 건수를 세면서 **아무것도 쓰지 않는지**
  - 체크를 풀면 어느 방향으로도 스위치가 움직이지 않는지
  - **한 복지관의 가져오기가 다른 복지관 스위치를 건드리지 않는지**
  - 탐님이 주신 실제 파일을 넣은 뒤 **키오스크가 그 서비스를 실제로 아는지**
- 마지막 항목이 핵심입니다. 이전까지의 시험은 **키오스크가 아무것도 안내하지
  못하는 상태에서도 전부 통과**했고, 그래서 이 문제가 시험을 통과해 버렸습니다.

## 6. 문서

- `PROJECT.md` 9장(비용) — 캐싱 관련 내용을 실제 측정값으로 교체.
- `NOTES-FOR-TOM-COMPLETION.md` — 이번 완료 보고.

---

## 아직 안 한 것

- **실시간 연동(날씨·병원·버스)** — 키가 들어와야 시작합니다.
- **배포** — 아직 아무 곳에도 올라가 있지 않습니다.
- **비밀번호 교체** — DB 1개, 로그인 3개. 공개 주소가 생기기 전에.
- **키오스크 가로 화면(21.5")**
- **마이크** — 안드로이드 크롬/웹뷰 확인 (하드웨어 담당자 문의 필요)
